

name: CI
on:
 
  push:
   branches: 
    - feature-ms-jorgecarvajal-mensaje
  workflow_dispatch:
 
jobs:
  build:
	
    runs-on: ubuntu-latest
		
    steps:
		
      - uses: actions/checkout@v3

      - name: BUILD
        run: |
          echo "BUILDING"
